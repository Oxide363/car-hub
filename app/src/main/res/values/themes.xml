<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme is a no-action-bar dark surface; the UI itself is drawn with Compose. -->
    <style name="Theme.CarHub" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0F0F12</item>
        <item name="android:navigationBarColor">#0F0F12</item>
        <item name="android:windowBackground">#0F0F12</item>
    </style>
</resources>
